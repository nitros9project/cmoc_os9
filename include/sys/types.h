#ifndef _SYS_TYPES_H
#define _SYS_TYPES_H

/**
 * @file sys/types.h
 * @brief Common scalar types used across the CMOC OS-9 C Library.
 */

/** @brief File offset type. */
typedef long off_t;
/** @brief Object and buffer size type. */
typedef int size_t;
/** @brief Clock-tick count type. */
typedef unsigned clock_t;

#endif
