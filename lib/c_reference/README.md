# C Reference Sources

This directory holds C versions of library routines that are useful as reference implementations while working on the CMOC OS-9 runtime library.

The files here are not part of the normal library build. They exist to preserve readable source logic for comparison, testing, or future rewrites of the assembly routines used by the runtime.
