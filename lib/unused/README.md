# Unused Library Sources

This directory holds library source files that are currently not used by the CMOC OS-9 runtime build.

The files are kept here instead of being deleted because they may still be useful as historical reference, for comparison against active implementations, or as starting points for future work.
